[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Optional: SPA-style fallback (safe to keep even if you don't use Vue Router yet)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200